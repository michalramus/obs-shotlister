import React from 'react'

export default function App(): React.JSX.Element {
  return (
    <div>
      <h1>OBS Queuer — Operator UI</h1>
      <p>Application shell — no features yet.</p>
    </div>
  )
}
